import { PREFIX } from "./common.constants";

export const CLASSNAME_PREFIX = PREFIX;
export const CSS_VARIABLE_PREFIX = PREFIX;

export const SCROLL_LOCK_CLASSNAME = `${CLASSNAME_PREFIX}--scroll-lock`;
